import React from 'react';
import { SpinnerContainer } from '../styles/SpinnerStyles';

const Spinner = () => (
  <SpinnerContainer>
    <div className="spinner"></div>
  </SpinnerContainer>
);

export default Spinner;